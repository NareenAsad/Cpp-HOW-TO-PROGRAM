#include <iostream>
using namespace std;
int main ()
{
   int a, b, c;
   cout<< “Enter first integer:”;
   cin>>a;
   cout<< “Enter second integer:”;
   cin>>b;
   cout<< “Enter third integer:”;
   cin>>c;
   if (a % b==0 && a % c==0)
   cout<< “First two integers are factor of second”;
   if (a % b !=0 && a % c != 0)
   cout<< “First two integers are not factor of second”;
    return 0;
}
